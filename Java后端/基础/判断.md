# Java 判断语句全面总结

## 📋 判断语句概述

判断语句用于根据条件决定程序的执行路径，是控制程序流程的基础。

---

## 🔀 if 语句

### 1. 基本 if 语句

```
// 语法
if (condition) {
    // 条件为true时执行的代码
}

// 示例
int score = 85;
if (score >= 60) {
    System.out.println("恭喜，你及格了！");
}
```

### 2. if-else 语句

```
// 语法
if (condition) {
    // 条件为true时执行
} else {
    // 条件为false时执行
}

// 示例
int age = 16;
if (age >= 18) {
    System.out.println("你已成年，可以观看此内容");
} else {
    System.out.println("你还未成年，禁止观看");
}
```

### 3. if-else if-else 语句

```
// 语法
if (condition1) {
    // 条件1为true时执行
} else if (condition2) {
    // 条件2为true时执行
} else if (condition3) {
    // 条件3为true时执行
} else {
    // 以上条件都不满足时执行
}

// 示例：成绩等级评定
int score = 78;
if (score >= 90) {
    System.out.println("优秀");
} else if (score >= 80) {
    System.out.println("良好");
} else if (score >= 70) {
    System.out.println("中等");
} else if (score >= 60) {
    System.out.println("及格");
} else {
    System.out.println("不及格");
}
```

### 4. 嵌套 if 语句

```
// 语法
if (condition1) {
    if (condition2) {
        // 两个条件都满足
    }
}

// 示例：用户登录验证
String username = "admin";
String password = "123456";
boolean rememberMe = true;

if (username != null && !username.isEmpty()) {
    if (password != null && !password.isEmpty()) {
        if (username.equals("admin") && password.equals("123456")) {
            System.out.println("登录成功");
            if (rememberMe) {
                System.out.println("已记住登录状态");
            }
        } else {
            System.out.println("用户名或密码错误");
        }
    } else {
        System.out.println("密码不能为空");
    }
} else {
    System.out.println("用户名不能为空");
}
```

### 5. 简化写法（单行语句）

```
// 只有一行语句时可以省略大括号
int number = 10;
if (number > 0) 
    System.out.println("正数");
else
    System.out.println("非正数");

// 但不推荐，容易出错
if (number > 0)
    System.out.println("正数");
    System.out.println("这个总是会执行");  // 不在if范围内！

// 推荐总是使用大括号
if (number > 0) {
    System.out.println("正数");
}
```

---

## 🔀 switch 语句

### 1. 基本 switch 语句

```
// 语法
switch (expression) {
    case value1:
        // 代码块1
        break;
    case value2:
        // 代码块2
        break;
    // 更多case...
    default:
        // 默认代码块
}

// 示例：星期判断
int day = 3;
switch (day) {
    case 1:
        System.out.println("星期一");
        break;
    case 2:
        System.out.println("星期二");
        break;
    case 3:
        System.out.println("星期三");
        break;
    case 4:
        System.out.println("星期四");
        break;
    case 5:
        System.out.println("星期五");
        break;
    case 6:
        System.out.println("星期六");
        break;
    case 7:
        System.out.println("星期日");
        break;
    default:
        System.out.println("无效的日期");
}
```

### 2. 多个 case 合并

```
// 示例：月份季节判断
int month = 5;
switch (month) {
    case 12:
    case 1:
    case 2:
        System.out.println("冬季");
        break;
    case 3:
    case 4:
    case 5:
        System.out.println("春季");
        break;
    case 6:
    case 7:
    case 8:
        System.out.println("夏季");
        break;
    case 9:
    case 10:
    case 11:
        System.out.println("秋季");
        break;
    default:
        System.out.println("无效的月份");
}
```

### 3. switch 表达式（Java 14+）

```
// 使用 -> 箭头语法
int day = 3;
switch (day) {
    case 1 -> System.out.println("星期一");
    case 2 -> System.out.println("星期二");
    case 3 -> System.out.println("星期三");
    case 4 -> System.out.println("星期四");
    case 5 -> System.out.println("星期五");
    default -> System.out.println("周末");
}

// 返回值表达式
String dayType = switch (day) {
    case 1, 2, 3, 4, 5 -> "工作日";
    case 6, 7 -> "周末";
    default -> "无效";
};
System.out.println(dayType);

// yield 返回值
int seasonCode = switch (month) {
    case 12, 1, 2 -> {
        System.out.println("冬季");
        yield 4;  // 返回4表示冬季
    }
    case 3, 4, 5 -> {
        System.out.println("春季");
        yield 1;
    }
    case 6, 7, 8 -> {
        System.out.println("夏季");
        yield 2;
    }
    case 9, 10, 11 -> {
        System.out.println("秋季");
        yield 3;
    }
    default -> 0;
};
```

### 4. 枚举类型 switch

```
enum Color { RED, GREEN, BLUE, YELLOW }

Color color = Color.RED;
switch (color) {
    case RED:
        System.out.println("红色");
        break;
    case GREEN:
        System.out.println("绿色");
        break;
    case BLUE:
        System.out.println("蓝色");
        break;
    default:
        System.out.println("其他颜色");
}
```

### 5. 字符串类型 switch

```
String language = "Java";
switch (language) {
    case "Java":
        System.out.println("Java是一种面向对象的编程语言");
        break;
    case "Python":
        System.out.println("Python是一种解释型脚本语言");
        break;
    case "JavaScript":
        System.out.println("JavaScript是一种脚本语言");
        break;
    default:
        System.out.println("未知语言");
}
```

---

## 🔀 三元运算符（条件运算符）

### 1. 基本用法

```
// 语法
variable = (condition) ? valueIfTrue : valueIfFalse;

// 示例
int a = 10, b = 20;
int max = (a > b) ? a : b;  // max = 20
System.out.println("最大值是: " + max);

// 示例：奇偶判断
int number = 7;
String result = (number % 2 == 0) ? "偶数" : "奇数";
System.out.println(number + " 是 " + result);
```

### 2. 嵌套三元运算符

```
// 示例：成绩等级判断
int score = 85;
String grade = (score >= 90) ? "优秀" : 
               (score >= 80) ? "良好" : 
               (score >= 70) ? "中等" : 
               (score >= 60) ? "及格" : "不及格";
System.out.println("成绩等级: " + grade);

// 示例：三个数找最大值
int x = 10, y = 20, z = 15;
int max = (x > y) ? (x > z ? x : z) : (y > z ? y : z);
System.out.println("最大值: " + max);
```

### 3. 在赋值和返回中使用

```
// 在赋值中使用
boolean isMember = true;
double discount = isMember ? 0.9 : 1.0;  // 会员9折，非会员原价
double price = 100.0;
double finalPrice = price * discount;
System.out.println("最终价格: " + finalPrice);

// 在返回中使用
public static String getLevel(int score) {
    return (score >= 90) ? "A" : 
           (score >= 80) ? "B" : 
           (score >= 70) ? "C" : 
           (score >= 60) ? "D" : "F";
}

// 在字符串拼接中使用
int age = 18;
String message = "你是" + (age >= 18 ? "成年" : "未成年") + "人";
System.out.println(message);
```

---

## 🔀 模式匹配（Java 16+）

### 1. instanceof 模式匹配

```
// 传统方式
Object obj = "Hello World";
if (obj instanceof String) {
    String str = (String) obj;  // 需要强制转换
    System.out.println(str.toUpperCase());
}

// 模式匹配方式
if (obj instanceof String str) {  // 自动转换
    System.out.println(str.toUpperCase());
}

// 结合条件判断
if (obj instanceof String str && str.length() > 5) {
    System.out.println("长字符串: " + str);
}
```

### 2. switch 模式匹配（Java 17预览）

```
// 传统方式
Object obj = 123;
if (obj instanceof Integer) {
    Integer i = (Integer) obj;
    System.out.println("整数: " + i);
} else if (obj instanceof String) {
    String s = (String) obj;
    System.out.println("字符串: " + s);
} else {
    System.out.println("其他类型");
}

// 模式匹配方式
Object obj = 123;
String result = switch (obj) {
    case Integer i -> "整数: " + i;
    case String s -> "字符串: " + s;
    case null -> "空值";
    default -> "其他类型";
};
System.out.println(result);
```

---

## 💡 实战应用示例

### 示例1：计算器程序

```
import java.util.Scanner;

public class Calculator {
    public static void main(String[] args) {
        Scanner scanner = new Scanner(System.in);
        
        System.out.print("请输入第一个数字: ");
        double num1 = scanner.nextDouble();
        
        System.out.print("请输入运算符(+, -, *, /): ");
        char operator = scanner.next().charAt(0);
        
        System.out.print("请输入第二个数字: ");
        double num2 = scanner.nextDouble();
        
        double result = 0;
        boolean valid = true;
        
        // 使用switch进行运算符判断
        switch (operator) {
            case '+':
                result = num1 + num2;
                break;
            case '-':
                result = num1 - num2;
                break;
            case '*':
                result = num1 * num2;
                break;
            case '/':
                if (num2 != 0) {
                    result = num1 / num2;
                } else {
                    System.out.println("错误：除数不能为0");
                    valid = false;
                }
                break;
            default:
                System.out.println("错误：无效的运算符");
                valid = false;
        }
        
        if (valid) {
            System.out.println("计算结果: " + num1 + " " + operator + " " + num2 + " = " + result);
        }
        
        scanner.close();
    }
}
```

### 示例2：用户权限系统

```
public class PermissionSystem {
    public static void main(String[] args) {
        String role = "ADMIN";  // 角色：ADMIN, EDITOR, USER, GUEST
        String resource = "settings";  // 资源：dashboard, settings, reports
        
        // 检查权限
        boolean hasPermission = false;
        
        // 使用嵌套if进行权限检查
        if (role.equals("ADMIN")) {
            // 管理员拥有所有权限
            hasPermission = true;
        } else if (role.equals("EDITOR")) {
            // 编辑者的权限
            if (resource.equals("dashboard") || resource.equals("reports")) {
                hasPermission = true;
            } else if (resource.equals("settings")) {
                hasPermission = false;
            }
        } else if (role.equals("USER")) {
            // 普通用户的权限
            hasPermission = resource.equals("dashboard");
        } else if (role.equals("GUEST")) {
            // 游客只有只读权限
            hasPermission = false;
        }
        
        // 使用三元运算符生成消息
        String message = hasPermission ? 
            "权限验证通过，可以访问" + resource : 
            "权限不足，无法访问" + resource;
        
        System.out.println(message);
        
        // 使用switch优化版本
        boolean hasPermission2 = switch (role) {
            case "ADMIN" -> true;
            case "EDITOR" -> resource.equals("dashboard") || resource.equals("reports");
            case "USER" -> resource.equals("dashboard");
            case "GUEST" -> false;
            default -> false;
        };
        
        System.out.println("优化版权限检查: " + (hasPermission2 ? "通过" : "拒绝"));
    }
}
```

### 示例3：日期工具类

```
public class DateUtils {
    public static void main(String[] args) {
        int year = 2024;
        int month = 2;  // 2月
        
        // 判断闰年
        boolean isLeapYear = isLeapYear(year);
        System.out.println(year + "年是" + (isLeapYear ? "闰年" : "平年"));
        
        // 获取月份天数
        int days = getDaysInMonth(year, month);
        System.out.println(year + "年" + month + "月有" + days + "天");
        
        // 判断季度
        String quarter = getQuarter(month);
        System.out.println(month + "月属于第" + quarter + "季度");
    }
    
    /**
     * 判断是否为闰年
     */
    public static boolean isLeapYear(int year) {
        // 闰年规则：能被4整除但不能被100整除，或者能被400整除
        if (year % 400 == 0) {
            return true;
        } else if (year % 100 == 0) {
            return false;
        } else {
            return year % 4 == 0;
        }
        
        // 简化版本：
        // return (year % 4 == 0 && year % 100 != 0) || (year % 400 == 0);
    }
    
    /**
     * 获取月份天数
     */
    public static int getDaysInMonth(int year, int month) {
        // 使用switch语句
        switch (month) {
            case 1: case 3: case 5: case 7: case 8: case 10: case 12:
                return 31;
            case 4: case 6: case 9: case 11:
                return 30;
            case 2:
                return isLeapYear(year) ? 29 : 28;
            default:
                throw new IllegalArgumentException("无效的月份: " + month);
        }
    }
    
    /**
     * 获取季度
     */
    public static String getQuarter(int month) {
        // 使用if-else if语句
        if (month >= 1 && month <= 3) {
            return "一";
        } else if (month >= 4 && month <= 6) {
            return "二";
        } else if (month >= 7 && month <= 9) {
            return "三";
        } else if (month >= 10 && month <= 12) {
            return "四";
        } else {
            return "无效";
        }
        
        // 使用三元运算符版本
        // return (month >= 1 && month <= 3) ? "一" :
        //        (month >= 4 && month <= 6) ? "二" :
        //        (month >= 7 && month <= 9) ? "三" :
        //        (month >= 10 && month <= 12) ? "四" : "无效";
    }
}
```

### 示例4：登录验证系统

```
import java.util.HashMap;
import java.util.Map;

public class LoginSystem {
    // 模拟用户数据库
    private static Map<String, User> users = new HashMap<>();
    
    static {
        users.put("admin", new User("admin", "admin123", true));
        users.put("user1", new User("user1", "pass123", false));
        users.put("guest", new User("guest", "guest", true));
    }
    
    static class User {
        String username;
        String password;
        boolean enabled;  // 账户是否启用
        
        User(String username, String password, boolean enabled) {
            this.username = username;
            this.password = password;
            this.enabled = enabled;
        }
    }
    
    public static void main(String[] args) {
        String username = "admin";
        String password = "admin123";
        
        // 多层验证逻辑
        String loginResult = authenticate(username, password);
        System.out.println("登录结果: " + loginResult);
    }
    
    public static String authenticate(String username, String password) {
        // 第一步：检查用户名是否为空
        if (username == null || username.trim().isEmpty()) {
            return "用户名不能为空";
        }
        
        // 第二步：检查密码是否为空
        if (password == null || password.trim().isEmpty()) {
            return "密码不能为空";
        }
        
        // 第三步：检查用户是否存在
        User user = users.get(username);
        if (user == null) {
            return "用户不存在";
        }
        
        // 第四步：检查账户是否启用
        if (!user.enabled) {
            return "账户已被禁用";
        }
        
        // 第五步：检查密码是否正确
        if (!user.password.equals(password)) {
            return "密码错误";
        }
        
        // 第六步：检查用户角色
        String role = determineRole(username);
        String welcomeMessage = switch (role) {
            case "ADMIN" -> "欢迎管理员登录系统";
            case "USER" -> "欢迎用户登录系统";
            case "GUEST" -> "欢迎访客登录系统";
            default -> "欢迎登录系统";
        };
        
        return "登录成功！" + welcomeMessage;
    }
    
    public static String determineRole(String username) {
        // 使用if-else语句判断角色
        if (username.startsWith("admin")) {
            return "ADMIN";
        } else if (username.startsWith("user")) {
            return "USER";
        } else if (username.startsWith("guest")) {
            return "GUEST";
        } else {
            return "UNKNOWN";
        }
    }
}
```

---

## ⚡ 性能与最佳实践

### 1. 性能优化

```
// 1. 条件顺序优化
// 将最可能为true的条件放在前面
if (isUserActive() && hasPermission() && isValidData()) {
    // 处理
}

// 2. 短路求值利用
if (list != null && !list.isEmpty() && list.get(0) != null) {
    // 如果list为null，后面的判断不会执行
}

// 3. switch vs if-else
// 当case较多时，switch通常比if-else效率高
int value = 100;
// 使用switch（编译器可能优化为跳转表）
switch (value) {
    case 1: break;
    case 2: break;
    // ... 多个case
}

// 4. 避免重复计算
// 不好
if (calculateComplexValue() > 0 && calculateComplexValue() < 100) {
    // 计算了两次
}

// 好
double value = calculateComplexValue();
if (value > 0 && value < 100) {
    // 只计算一次
}
```

### 2. 可读性最佳实践

```
// 1. 使用有意义的条件变量
// 不好
if (x > 5 && y < 10) {
    // ...
}

// 好
boolean isInRange = x > 5 && y < 10;
boolean isValidData = isDataComplete && isDataConsistent;
if (isInRange && isValidData) {
    // ...
}

// 2. 避免深层嵌套
// 不好
if (condition1) {
    if (condition2) {
        if (condition3) {
            // 业务逻辑
        }
    }
}

// 好：使用卫语句提前返回
if (!condition1) {
    return;
}
if (!condition2) {
    return;
}
if (!condition3) {
    return;
}
// 业务逻辑

// 3. 使用枚举代替魔术数字
// 不好
if (status == 1) {
    // ...
} else if (status == 2) {
    // ...
}

// 好
enum Status { ACTIVE, INACTIVE, PENDING }
if (status == Status.ACTIVE) {
    // ...
}
```

### 3. 常见错误避免

```
// 1. 赋值(=) vs 比较(==)
int x = 5;
if (x = 10) {  // 编译错误，Java中if条件必须是boolean
    // ...
}

boolean flag = false;
if (flag = true) {  // 正确，但可能不是你的本意
    // 总是会执行
}

// 2. 浮点数比较
double a = 0.1 + 0.2;
double b = 0.3;
if (a == b) {  // 可能为false，浮点数精度问题
    System.out.println("相等");
}

// 应该使用容差比较
double epsilon = 0.000001;
if (Math.abs(a - b) < epsilon) {
    System.out.println("近似相等");
}

// 3. 字符串比较
String str1 = "hello";
String str2 = new String("hello");

if (str1 == str2) {  // false，比较的是引用
    // ...
}

if (str1.equals(str2)) {  // true，比较的是内容
    // ...
}
```

---

## 📊 判断语句选择指南

|场景|推荐使用|原因|
|---|---|---|
|2-3个条件分支|if-else|简单直观|
|4+个条件分支，基于单个值|switch|可读性好，性能可能更好|
|需要返回值|三元运算符/switch表达式|简洁|
|复杂条件组合|if-else|灵活|
|枚举类型判断|switch|类型安全，可读性好|
|模式匹配|instanceof模式匹配|减少强制转换|
|提前退出|卫语句(guard clauses)|减少嵌套，提高可读性|

### 选择流程

```
// 决策流程
if (条件是简单相等比较 && 分支数量较多) {
    使用switch语句;
} else if (需要返回值 && 分支简单) {
    使用三元运算符;
} else if (涉及类型判断) {
    使用模式匹配;
} else if (条件复杂 || 需要范围判断) {
    使用if-else语句;
} else {
    根据可读性选择;
}
```

---

## 🎯 总结

### 核心判断语句

1. **if-else语句**：最灵活，适用于任何条件判断
    
2. **switch语句**：适用于多分支选择，基于单个值的相等比较
    
3. **三元运算符**：简单的条件赋值，代码简洁
    
4. **模式匹配**：Java 16+，类型安全的类型检查和转换
    

### 关键要点

1. **if-else**​ 是基础，**switch**​ 是多分支优化
    
2. **switch表达式**（Java 14+）更安全，功能更强
    
3. **三元运算符**适合简单赋值，避免复杂嵌套
    
4. **卫语句**可以减少嵌套，提高可读性
    
5. 根据具体场景选择最合适的判断方式
    

### 最佳实践

1. 保持条件简单，使用有意义的变量名
    
2. 避免深层嵌套，使用卫语句提前返回
    
3. 使用枚举代替魔术数字/字符串
    
4. 注意边界条件和空值处理
    
5. 考虑代码的可读性和维护性
    

掌握这些判断语句的使用技巧，能够让你写出更清晰、更高效、更易维护的Java代码。
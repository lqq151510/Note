# Java 基础知识全面总结

## 📋 Java 语言概述

Java 是一种面向对象的编程语言，具有"一次编写，到处运行"的跨平台特性。它由 Sun Microsystems 于 1995 年推出，现属于 Oracle 公司。

---

## 🔤 字面量

字面量是在代码中直接表示固定值的符号。

### 1. 整数字面量

```
int decimal = 100;        // 十进制
int octal = 0144;         // 八进制，以0开头
int hexadecimal = 0x64;   // 十六进制，以0x或0X开头
int binary = 0b1100100;   // 二进制，以0b或0B开头
long bigNumber = 100L;    // long类型，加L或l
```

### 2. 浮点数字面量

```
float pi = 3.14f;         // float类型，加f或F
double rate = 3.14;       // double类型
double scientific = 3.0e8; // 科学计数法
```

### 3. 字符字面量

```
char letter = 'A';        // 单个字符
char chinese = '中';      // 汉字
char escape = '\n';       // 转义字符
char unicode = '\u0041';  // Unicode字符，表示'A'
```

**常用转义字符**：

- `\n`- 换行
    
- `\t`- 制表符
    
- `\\`- 反斜杠
    
- `\'`- 单引号
    
- `\"`- 双引号
    
- `\r`- 回车
    

### 4. 字符串字面量

```
String str1 = "Hello";           // 字符串
String str2 = "Hello\nWorld";    // 包含转义字符
String str3 = "";                // 空字符串
String multiLine = """
    This is a
    multi-line
    string""";                  // 多行字符串（Java 13+）
```

### 5. 布尔字面量

```
boolean isTrue = true;
boolean isFalse = false;
```

### 6. null 字面量

```
String str = null;  // 表示没有引用任何对象
```

---

## 📊 变量

变量是存储数据的内存单元，具有名称、类型和值。

### 1. 变量声明

```
// 声明变量
int age;
String name;
double price;

// 声明并初始化
int count = 0;
String message = "Hello";
boolean isActive = true;
```

### 2. 变量类型

```
// 局部变量（在方法内部）
void method() {
    int localVar = 10;  // 必须初始化
}

// 实例变量（在类内部，方法外部）
class MyClass {
    int instanceVar;    // 有默认值
}

// 类变量（静态变量）
class MyClass {
    static int classVar;  // 有默认值
}
```

### 3. 变量作用域

```
public class ScopeExample {
    int globalVar = 10;  // 实例变量
    
    void method() {
        int localVar = 20;  // 局部变量
        
        if (true) {
            int blockVar = 30;  // 块级变量
            System.out.println(blockVar);
        }
        // System.out.println(blockVar);  // 错误，超出作用域
    }
}
```

---

## 💬 注释

注释用于解释代码，不会被编译器执行。

### 1. 单行注释

```
// 这是单行注释
int x = 5;  // 也可以放在代码后面
```

### 2. 多行注释

```
/*
 * 这是多行注释
 * 可以跨越多行
 * 通常用于方法说明
 */
public void calculate() {
    // 方法体
}
```

### 3. 文档注释

```
/**
 * 计算两个数的和
 * 
 * @param a 第一个加数
 * @param b 第二个加数
 * @return 两个数的和
 * @throws IllegalArgumentException 如果参数无效
 */
public int add(int a, int b) {
    return a + b;
}
```

**常用文档标签**：

- `@param`- 方法参数说明
    
- `@return`- 返回值说明
    
- `@throws`- 抛出异常说明
    
- `@see`- 参考链接
    
- `@deprecated`- 已弃用
    
- `@since`- 从哪个版本开始
    
- `@version`- 版本信息
    
- `@author`- 作者
    

---

## 🔑 关键字

Java 中具有特殊含义的保留字，不能用作标识符。

### 1. 访问控制

```
public    // 公共的
private   // 私有的
protected // 受保护的
```

### 2. 类、方法和变量修饰符

```
abstract  // 抽象的
static    // 静态的
final     // 最终的
synchronized  // 同步的
volatile  // 易变的
transient // 瞬态的
native    // 本地的
```

### 3. 类定义相关

```
class     // 类
interface // 接口
enum      // 枚举
extends   // 继承
implements // 实现
```

### 4. 基本类型

```
byte, short, int, long
float, double
char, boolean
void      // 无返回值
```

### 5. 流程控制

```
if, else, switch, case, default
for, while, do, break, continue
return
try, catch, finally, throw, throws
assert    // 断言
```

### 6. 包相关

```
package   // 包
import    // 导入
```

### 7. 其他

```
new       // 创建对象
this      // 当前对象
super     // 父类对象
instanceof  // 类型检查
```

**注意**：`const`和 `goto`是保留关键字，但在 Java 中未使用。

---

## 📝 标识符

标识符是给类、方法、变量等命名的符号。

### 1. 命名规则

```
// 合法标识符
int age;
String userName;
double _price;
boolean $isValid;
int MAX_VALUE;

// 非法标识符
int 123abc;     // 不能以数字开头
int my-name;    // 不能包含减号
double class;   // 不能是关键字
```

**标识符规则**：

- 以字母、下划线(_)或美元符号($)开头
    
- 后续字符可以是字母、数字、下划线或美元符号
    
- 不能是 Java 关键字
    
- 区分大小写
    
- 长度不限
    

### 2. 命名约定

```
// 类名：大驼峰
class MyClass {}
class UserService {}

// 方法名和变量名：小驼峰
void calculateArea() {}
int studentCount = 0;

// 常量名：全大写，用下划线分隔
final int MAX_SIZE = 100;
static final String DEFAULT_NAME = "Unknown";

// 包名：全小写，用点分隔
package com.example.myapp;
```

### 3. 有意义命名的重要性

```
// 不好
int a;          // 无意义
int d;          // 不知道是什么
int calc(int x) { return x * 2; }  // 不清楚计算什么

// 好
int age;        // 表示年龄
int distance;   // 表示距离
int calculateCircleArea(int radius) {  // 计算圆面积
    return (int)(Math.PI * radius * radius);
}
```

---

## 📈 数据类型

### 1. 基本数据类型

|类型|大小|范围|默认值|包装类|
|---|---|---|---|---|
|`byte`|1字节|-128 ~ 127|0|Byte|
|`short`|2字节|-32768 ~ 32767|0|Short|
|`int`|4字节|-2³¹ ~ 2³¹-1|0|Integer|
|`long`|8字节|-2⁶³ ~ 2⁶³-1|0L|Long|
|`float`|4字节|±3.4E-38 ~ ±3.4E+38|0.0f|Float|
|`double`|8字节|±1.7E-308 ~ ±1.7E+308|0.0d|Double|
|`char`|2字节|0 ~ 65535|'\u0000'|Character|
|`boolean`|1位|true/false|false|Boolean|

### 2. 引用数据类型

```
// 类类型
String str = "Hello";

// 接口类型
List<String> list = new ArrayList<>();

// 数组类型
int[] numbers = {1, 2, 3};
String[] names = new String[5];

// 枚举类型
enum Color { RED, GREEN, BLUE }
Color color = Color.RED;
```

### 3. 类型转换

```
// 自动类型转换（小范围 → 大范围）
int intValue = 100;
long longValue = intValue;  // 自动转换
double doubleValue = intValue;  // 自动转换

// 强制类型转换（大范围 → 小范围）
double price = 9.99;
int intPrice = (int)price;  // 强制转换，结果为9

// 包装类转换
int num = Integer.parseInt("123");
String str = Integer.toString(456);
```

### 4. 类型检查

```
Object obj = "Hello";
if (obj instanceof String) {
    String str = (String)obj;
    System.out.println(str.length());
}
```

---

## 🔧 运算符

### 1. 算术运算符

```
int a = 10, b = 3;

int sum = a + b;      // 13，加法
int difference = a - b;  // 7，减法
int product = a * b;  // 30，乘法
int quotient = a / b;  // 3，除法
int remainder = a % b;  // 1，取余

// 自增自减
int x = 5;
x++;  // 后自增，x=6
++x;  // 前自增，x=7
x--;  // 后自减，x=6
--x;  // 前自减，x=5
```

### 2. 赋值运算符

```
int x = 10;  // 基本赋值
x += 5;      // 等同于 x = x + 5
x -= 3;      // 等同于 x = x - 3
x *= 2;      // 等同于 x = x * 2
x /= 4;      // 等同于 x = x / 4
x %= 3;      // 等同于 x = x % 3
```

### 3. 比较运算符

```
int a = 10, b = 20;

boolean eq = (a == b);  // false，等于
boolean ne = (a != b);  // true，不等于
boolean gt = (a > b);   // false，大于
boolean lt = (a < b);   // true，小于
boolean ge = (a >= b);  // false，大于等于
boolean le = (a <= b);  // true，小于等于
```

### 4. 逻辑运算符

```
boolean x = true, y = false;

boolean and = x && y;  // false，逻辑与
boolean or = x || y;   // true，逻辑或
boolean not = !x;      // false，逻辑非
```

### 5. 位运算符

```
int a = 5;  // 二进制：0101
int b = 3;  // 二进制：0011

int and = a & b;   // 0001 = 1，按位与
int or = a | b;    // 0111 = 7，按位或
int xor = a ^ b;   // 0110 = 6，按位异或
int not = ~a;      // 按位取反
int leftShift = a << 1;  // 1010 = 10，左移
int rightShift = a >> 1;  // 0010 = 2，右移
int unsignedShift = a >>> 1;  // 无符号右移
```

### 6. 条件运算符（三元运算符）

```
int score = 85;
String result = (score >= 60) ? "及格" : "不及格";
// result = "及格"
```

### 7. 运算符优先级

1. `()`括号
    
2. `++``--`自增自减
    
3. `!``~`逻辑非、按位非
    
4. `*``/``%`乘除取余
    
5. `+``-`加减
    
6. `<<``>>``>>>`移位
    
7. `<``<=``>``>=`比较
    
8. `==``!=`相等比较
    
9. `&`按位与
    
10. `^`按位异或
    
11. `|`按位或
    
12. `&&`逻辑与
    
13. `||`逻辑或
    
14. `?:`条件运算符
    
15. `=`赋值
    

---

## 📄 字符串

### 1. 字符串创建

```
// 字面量创建（推荐）
String str1 = "Hello";

// 构造方法创建
String str2 = new String("Hello");

// 从字符数组创建
char[] chars = {'H', 'e', 'l', 'l', 'o'};
String str3 = new String(chars);

// 从字节数组创建
byte[] bytes = {72, 101, 108, 108, 111};
String str4 = new String(bytes);
```

### 2. 字符串不可变性

```
String s1 = "Hello";
String s2 = s1;  // s2和s1引用同一个对象
s1 = s1 + " World";  // 创建新字符串，s2不变

System.out.println(s1);  // "Hello World"
System.out.println(s2);  // "Hello"
```

### 3. 常用字符串方法

```
String str = "Hello World";

// 获取信息
int length = str.length();  // 11
boolean isEmpty = str.isEmpty();  // false
char ch = str.charAt(0);  // 'H'

// 查找
int index1 = str.indexOf('o');  // 4
int index2 = str.lastIndexOf('o');  // 7
boolean contains = str.contains("World");  // true
boolean starts = str.startsWith("Hello");  // true
boolean ends = str.endsWith("World");  // true

// 子字符串
String sub1 = str.substring(6);  // "World"
String sub2 = str.substring(0, 5);  // "Hello"

// 修改
String upper = str.toUpperCase();  // "HELLO WORLD"
String lower = str.toLowerCase();  // "hello world"
String trimmed = "  Hello  ".trim();  // "Hello"
String replaced = str.replace('l', 'L');  // "HeLLo WorLd"
String concat = str.concat("!!!");  // "Hello World!!!"

// 分割
String[] parts = str.split(" ");  // ["Hello", "World"]
String joined = String.join("-", "Hello", "World");  // "Hello-World"

// 比较
boolean eq1 = str.equals("Hello World");  // true
boolean eq2 = str.equalsIgnoreCase("hello world");  // true
int compare = str.compareTo("Hello");  // 正数
```

### 4. 字符串格式化

```
// 传统方式
String formatted1 = String.format("姓名：%s，年龄：%d", "张三", 25);

// Java 15+ 文本块
String json = """
    {
        "name": "张三",
        "age": 25
    }
    """;
```

### 5. 字符串构建器

```
// StringBuilder（非线程安全，性能好）
StringBuilder sb1 = new StringBuilder();
sb1.append("Hello");
sb1.append(" ");
sb1.append("World");
String result1 = sb1.toString();  // "Hello World"

// StringBuffer（线程安全）
StringBuffer sb2 = new StringBuffer();
sb2.append("Hello");
sb2.append(" ");
sb2.append("World");
String result2 = sb2.toString();  // "Hello World"
```

### 6. 字符串比较注意

```
String s1 = "Hello";
String s2 = "Hello";
String s3 = new String("Hello");

System.out.println(s1 == s2);  // true，字符串常量池
System.out.println(s1 == s3);  // false，不同对象
System.out.println(s1.equals(s3));  // true，内容相同
```

---

## 💡 最佳实践总结

### 1. 命名规范

```
// 遵循约定
class UserService {  // 类名：大驼峰
    private static final int MAX_RETRY = 3;  // 常量：全大写
    
    public void getUserInfo() {  // 方法名：小驼峰
        int userId = 123;  // 变量名：小驼峰
    }
}
```

### 2. 字符串使用

```
// 使用字面量创建字符串
String str = "Hello";  // 而不是 new String("Hello")

// 多次拼接使用StringBuilder
StringBuilder sb = new StringBuilder();
for (int i = 0; i < 100; i++) {
    sb.append(i);
}
String result = sb.toString();

// 使用equals比较字符串内容
if ("test".equals(str)) {  // 防止空指针
    // 处理
}
```

### 3. 类型使用

```
// 明确类型
int count = 0;  // 基本类型，性能好
Integer total = null;  // 包装类，需要null时使用

// 注意自动装箱拆箱性能
long sum = 0L;  // 使用基本类型
for (int i = 0; i < 1000000; i++) {
    sum += i;  // 避免使用Long
}
```

### 4. 注释规范

```
/**
 * 用户服务类
 * 提供用户相关的业务操作
 * 
 * @author 张三
 * @version 1.0
 */
public class UserService {
    
    /**
     * 根据用户ID获取用户信息
     * 
     * @param userId 用户ID
     * @return 用户信息，如果不存在返回null
     * @throws IllegalArgumentException 如果用户ID小于0
     */
    public User getUserById(int userId) {
        if (userId < 0) {
            throw new IllegalArgumentException("用户ID不能为负数");
        }
        // 实现逻辑
        return null;
    }
}
```

---

## 🎯 核心要点回顾

1. **字面量**：直接表示值的符号，如 `100`、`"Hello"`、`true`
    
2. **变量**：存储数据的内存单元，需要声明类型
    
3. **注释**：解释代码，不执行，有单行、多行、文档注释
    
4. **关键字**：Java 保留的特殊含义单词，不能用作标识符
    
5. **标识符**：给类、方法、变量等的命名，遵循命名规则
    
6. **数据类型**：基本类型（8种）和引用类型，决定数据的存储和操作
    
7. **运算符**：执行操作的符号，有算术、比较、逻辑、赋值等
    
8. **字符串**：不可变字符序列，常用 `String`类处理文本
    

掌握这些基础知识是学习 Java 编程的第一步，是后续学习面向对象、集合、IO 等高级特性的基础。
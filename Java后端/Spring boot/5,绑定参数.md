`@RequestParam`是 Spring MVC 框架中一个非常重要的注解，用于将 HTTP 请求中的参数绑定到控制器方法的参数上。下面这个表格汇总了它的核心属性和用法，帮助你快速掌握。

|核心属性|作用|默认值|使用示例与说明|
|---|---|---|---|
|**`value`/ `name`**​|指定要绑定的请求参数名。|(无)|`@RequestParam("user_id") String userId`将 URL 中的 `user_id`参数绑定到 `userId`变量上。如果方法参数名与请求参数名一致，可省略此属性。|
|**`required`**​|指定参数是否必须传递。|`true`|`@RequestParam(required = false) String name`表示 `name`参数是可选的。若设置为 `true`（默认）但请求中缺少参数，会抛出异常导致 HTTP 400 错误。|
|**`defaultValue`**​|设置参数默认值（当请求未提供参数或参数值为空时使用）。|(无)|`@RequestParam(defaultValue = "guest") String username`。**特别注意：一旦设置了 `defaultValue`，就隐含了 `required = false`的行为**，即使没有显式设置。|

### 💡 核心用法详解

**1. 基本绑定**

最直接的用法是将请求参数绑定到方法参数。如果名称一致，可以简化写法。

```
@GetMapping("/greet")
public String greet(@RequestParam String name) {
    return "Hello, " + name;
}
```

访问 `/greet?name=Alice`，会返回 "Hello, Alice"。

**2. 处理可选参数与默认值**

这是 `@RequestParam`非常实用的功能，能有效增强接口的健壮性。

- **场景1：参数可选，不存在时为 `null`**
    
    适用于对象类型（如 `String`, `Integer`）。
    
    ```
    @GetMapping("/info")
    public String getInfo(@RequestParam(required = false) String note) {
        if (note != null) {
            return "Info: " + note;
        }
        return "No additional info.";
    }
    ```
    
- **场景2：参数可选，并提供默认值**
    
    适用于所有类型，是更推荐的做法。
    
    ```
    @GetMapping("/search")
    public String search(
        @RequestParam(defaultValue = "1") int page,
        @RequestParam(defaultValue = "10") int size) {
        return String.format("Searching: page %d, size %d", page, size);
    }
    ```
    
    **重要提示**：对于基本数据类型（如 `int`），如果参数可选且未设置默认值，必须使用其包装类（如 `Integer`），否则将 `null`赋给 `int`会导致错误。
    

### 🛠️ 进阶用法

**1. 绑定到集合类型**

当需要接收同一个参数名的多个值时（如多选框），可以绑定到数组或 `List`。

```
@GetMapping("/filters")
public String applyFilters(@RequestParam List<String> category) {
    // 请求示例：/filters?category=books&category=electronics
    return "Filtering by categories: " + category; // 输出: [books, electronics]
}
```

**2. 绑定所有参数到 Map**

如果需要获取全部请求参数，可以绑定到 `Map`。

```
@GetMapping("/params")
public String getAllParams(@RequestParam Map<String, String> allParams) {
    return "All parameters: " + allParams.toString();
}
```

**3. 处理表单提交**

`@RequestParam`同样适用于处理 POST 请求中的表单数据（`application/x-www-form-urlencoded`）。

```
@PostMapping("/login")
public String login(@RequestParam String username, 
                    @RequestParam String password) {
    // 处理登录逻辑
    return "Login attempt for: " + username;
}
```

### ⚠️ 常见问题与技巧

- **类型转换异常**：如果请求参数值无法转换为方法参数的类型（如将 "abc" 转换为 `int`），Spring 会抛出 `TypeMismatchException`。确保前端传递的数据类型正确。
    
- **与 `@PathVariable`的区别**：
    
    - `@RequestParam`用于获取**查询参数**（即URL中 `?`后面的 `key=value`对）。
        
    - `@PathVariable`用于获取**URL路径模板**中的变量（如 `/users/{id}`中的 `{id}`）。
        
    
- **与 `@RequestBody`的区别**：这是两个最常见的混淆点。
    
    - `@RequestParam`主要用于获取**查询字符串和表单数据**（简单的键值对）。
        
    - `@RequestBody`用于将**请求体**（通常是 JSON 或 XML）整体绑定到一个复杂的 Java 对象上。
        
    

|特性|`@RequestParam`|`@RequestBody`|
|---|---|---|
|**数据来源**​|URL 查询字符串(Query String) 或 表单数据(Form Data)|请求体 (Request Body)|
|**常见Content-Type**​|`application/x-www-form-urlencoded`|`application/json`, `application/xml`|
|**数据绑定**​|绑定单个参数或键值对|将整个请求体（如JSON）映射到一个自定义对象|

### 💎 最佳实践小结

1. **显式使用**：即使参数名一致，也建议显式加上 `@RequestParam`注解，使代码意图更清晰。
    
2. **优先使用包装类**：对于可选的数值型参数，优先使用 `Integer`, `Boolean`等包装类，以避免潜在的空值问题。
    
3. **善用默认值**：为可选参数设置合理的 `defaultValue`，可以提高 API 的容错性和用户体验。
    
4. **保持简洁**：如果一个方法有过多（如超过5个）`@RequestParam`参数，考虑将其封装成一个自定义对象，并使用 `@ModelAttribute`进行绑定，这会使代码更整洁。
    

希望这份详细的解释能帮助你全面地理解和使用 `@RequestParam`注解。